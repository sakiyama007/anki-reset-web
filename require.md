#1
ダークモードのとき、選択するとボタンが白くなるが、文字ももともと白いので見えなくなる。これを修正して
カード入力画面では、遷移すると自動で表面のカードにカーソルが合わさってて、キーボードをたたくだけで入力できるようにして。

#2
学習画面で、任意の場所をクリックすると答えが表示されるようにして。また、エンターキーでも答え表示、そして分類もキーボードでできるようにして。

#3
フォルダの複数選択で、親ファイルが選択されたら子ファイルにもチェックがつくようにして
評価ボタンは　k l ; :　にして

#4
カード入力画面では、遷移すると自動で表面のカードにカーソルが合わさってて、キーボードをたたくだけで入力できるようにして。
googleアカウント連携を可能にして

#5
同期の場合は、決してデータは失われないようにして。つまり、例えば2デバイスで同期する場合、A固有のデータをBに送ることでB固有データが消えないようにして
学習タブでも、親ふぉるだの選択で子フォルダも選択されるように修正して

#6
サブフォルダやカードを追加したときに、もとのがめんに戻ってまたフォルダを展開しなおさないといけないのが面倒なので、追加した後は追加する直前の画面が表示されるように修正して

#7
学習モードのとき、答えを表示ボタンが今はenterのみだが、kl;:キーならどれ押しても答えを表示するようにして
<1minのときは、次のカードに表示するのではなく、1min経ってから表示するようにしてください

#8
<1minは25second以後の表示に変更してください

#9
学習モードにて、評価後に次のカードが出てくるとき、一瞬次のかーどの裏面が見えるというバグがある。修正して    

#10
同期で、学習状況が同期されていなければそれも同期するようにして。その際、日本時間で状況を管理するようにして
